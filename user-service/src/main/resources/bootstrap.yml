# userservice-dev.yaml   nacos服务器上配置的文件id
# 服务名-环境(profiles.active).后缀名
spring:
  application:
    name: userservice #服务名
  profiles:
    #active: dev #环境
  cloud:
    nacos:
      server-addr: localhost:8848 #nacos服务器地址
      config:
        file-extension: yaml #文件后缀名
        #namespace: 54BFBB13362242E9BDAB33C8EEA79E60_dev